version: v0.1
testId: SampleAppTest
displayName: Sample Test
testPlan: SampleApp.jmx
description: 'Load test the sample app'
engineInstances: 1
env:
- name: webapp
  value: vismaloadtestdemo.azurewebsites.net

#failureCriteria: 
#    - p90(response_time_ms) > 100
#    - percentage(error) > 20